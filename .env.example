# URL del Cloudflare Worker (proxy CORS para el auditor de CMS).
# Despliega worker/ con `wrangler deploy` y pega aquí la URL.
# Sin esta variable, el auditor de WordPress no funciona.
VITE_WORKER_URL=https://pwnvader-cors-proxy.<tu-cuenta>.workers.dev
